# package.json note

This file defines the root Astro site's package metadata, scripts, and dependency surface.
It was updated to keep the declared Node engine aligned with the repo's Node 24 local and deploy tooling.
Keep this file aligned with actual project tooling so declared packages remain purposeful.
