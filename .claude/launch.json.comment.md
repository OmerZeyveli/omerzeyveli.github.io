<!-- Sidecar comment for .claude/launch.json -->
<!-- Scope: Claude Code preview tooling configuration -->
<!-- Audience: autonomous coding agents and maintainers -->

# .claude/launch.json

Defines the named dev-server configuration that Claude Code preview tooling
uses to start the Astro dev server (`pnpm dev` on port 4321). Pure JSON
cannot carry comments, so this sidecar documents it per the root header
policy in `AGENTS.md`.
