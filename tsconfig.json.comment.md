# tsconfig.json note

This file defines the TypeScript configuration shared across Astro, source files, and tooling.
It should stay aligned with the project's actual compile-time expectations and framework integrations.
Use this sidecar because the root policy requires header-equivalent documentation for pure JSON files.
