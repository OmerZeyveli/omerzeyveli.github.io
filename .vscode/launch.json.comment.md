# .vscode/launch.json note

This file defines the shared VS Code launch configuration for starting the Astro development server.
It is editor-only tooling and should stay aligned with the package scripts and local development command.
Use this sidecar because the root policy requires header-equivalent documentation for pure JSON files.
